{
  "name": "icampus-bot",
  "version": "1.0.0",
  "description": "",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "postinstall": "pip3 install -r requirements.txt --break-system-packages 2>/dev/null || pip3 install -r requirements.txt || pip install -r requirements.txt",
    "install:python": "pip3 install -r requirements.txt || pip install -r requirements.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0",
    "@whiskeysockets/baileys": "^7.0.0-rc14",
    "qrcode": "^1.5.4"
  }
}
